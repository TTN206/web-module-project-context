import {createContext} from 'react'; // import the `createContext` function from the react library and create our `ProductContext`.

export const ProductContext = createContext();
/*
we've created our `ProductContext` we can import into our `App.js`. Now we can start providing data across our application!
*/
